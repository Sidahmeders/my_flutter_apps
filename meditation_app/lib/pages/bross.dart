import 'package:flutter/material.dart';

class Bross extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('bross page')),
      body: Container(
        child: Text('cool bross a dont'),
      ),
    );
  }
}
