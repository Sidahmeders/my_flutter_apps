import 'package:flutter/material.dart';

class Snan extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Snan page'),),
    );
  }
}
